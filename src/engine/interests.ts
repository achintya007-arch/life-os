/**
 * Personalization: the player's interests turned into contract tasks.
 * (Filled in by the personalization system; empty until then.)
 */
import type { Candidate } from './contracts';
import type { GameState } from './types';

export function interestTasks(_state: GameState): Candidate[] {
  return [];
}
